FROM mysql:8.0

# Set root password (change to your preferred password)
ENV MYSQL_ROOT_PASSWORD=root

# Create a database named 'oopss' (change as needed)
ENV MYSQL_DATABASE=oopss

# Optionally, you can include initialization scripts or configuration here.
